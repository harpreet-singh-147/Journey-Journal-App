import React from "react";
import { View, StyleSheet, Text } from "react-native";

const JourneyDetails = () => {
	return (
		<View>
			<Text>This is the screen for add details</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default JourneyDetails;
